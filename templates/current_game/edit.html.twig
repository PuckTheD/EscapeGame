{% extends 'base.html.twig' %}

{% block title %}Edit CurrentGame{% endblock %}

{% block body %}
    <h1>Edit CurrentGame</h1>

    {{ include('current_game/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('current_game_index') }}">back to list</a>

    {{ include('current_game/_delete_form.html.twig') }}
{% endblock %}
