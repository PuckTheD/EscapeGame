{% extends 'base.html.twig' %}

{% block title %}Indice{% endblock %}

{% block body %}
    <h1>Indice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ indice.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ indice.titre }}</td>
            </tr>
            <tr>
                <th>Indice_txt</th>
                <td>{{ indice.indiceTxt }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('indice_index') }}">back to list</a>

    <a href="{{ path('indice_edit', {'id': indice.id}) }}">edit</a>

    {{ include('indice/_delete_form.html.twig') }}
{% endblock %}
