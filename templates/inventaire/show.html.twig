{% extends 'base.html.twig' %}

{% block title %}Inventaire{% endblock %}

{% block body %}
    <h1>Inventaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inventaire.id }}</td>
            </tr>
            <tr>
                <th>Indice_id</th>
                <td>{{ inventaire.indiceId }}</td>
            </tr>
            <tr>
                <th>User_id</th>
                <td>{{ inventaire.userId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('inventaire_index') }}">back to list</a>

    <a href="{{ path('inventaire_edit', {'id': inventaire.id}) }}">edit</a>

    {{ include('inventaire/_delete_form.html.twig') }}
{% endblock %}
