{% extends 'base.html.twig' %}

{% block title %}Thematique{% endblock %}

{% block body %}
    <h1>Thematique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ thematique.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ thematique.titre }}</td>
            </tr>
            <tr>
                <th>Theme</th>
                <td>{{ thematique.theme }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('thematique_index') }}">back to list</a>

    <a href="{{ path('thematique_edit', {'id': thematique.id}) }}">edit</a>

    {{ include('thematique/_delete_form.html.twig') }}
{% endblock %}
